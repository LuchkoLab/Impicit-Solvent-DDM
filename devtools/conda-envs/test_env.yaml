name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - toil
  - pandas
  - numpy
  - pyyaml
  - pytraj
  - ParmEd 
   
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

