name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - pandas
  - numpy
  - pyyaml
  - ParmEd 
   
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
     - toil
     - pytraj>=2.0.5
     #  - codecov