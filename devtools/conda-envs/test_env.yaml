name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - pandas
  - numpy
  - pyyaml
  - ParmEd 
   
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # conda-only installs
  - conda:
     - toil
     - "-c ambermd pytraj" 
     #  - codecov